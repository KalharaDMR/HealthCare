apiVersion: v1
kind: Service
metadata:
  name: healthcare-frontend
spec:
  type: NodePort          # so you can access it outside the cluster
  selector:
    app: healthcare-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080     # will be accessible at http://<minikube-ip>:30080